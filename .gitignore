# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build artifacts (local only, not for plugin distribution)
build/
*.plugin

# Build output in development (but include for plugin distribution)
# dist/ - intentionally excluded, needed for plugin

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Development
.pnpm-debug.log
docs/results/
docs/reports/
docs/lesson_learned/
docs/tasks/
docs/bugs/
!!docs/specs/
!!docs/plans/
!!docs/design/
.worktrees/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright-mcp/

# Claude local settings
.claude/PRPs/
.claude/settings.local.json
dashboard/.claude/

# Dashboard build output (rebuilt from source)
dashboard/dist/
dashboard/dist/.mcp.json

# Server build output (rebuilt from source)
server/dist/

# Local dev DBs
server/debug.sqlite
server/debug.sqlite-shm
server/debug.sqlite-wal

# Coverage reports
coverage/
**/coverage/

# Python bytecode (codex skills)
__pycache__/
*.pyc

# Root npm package staging — regenerated by `make npm-build` before publish
# (kept untracked locally; npm pack rebuilds it)
/dist/

# Agent / IDE scratch (local tooling, not project source)
.codex/
.agents/
.claude/worktrees/
.claude/skills/gsap-*

# Design-system scratch (large standalone mockups + screenshots)
/Claude DevTools Design System/
