# P0–P2 Bug Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix 6 confirmed bugs (5 missing `response.ok` guards + 1 optimistic-update P2) using strict TDD.

**Architecture:** Each hook/component already has or will get a `.test.ts(x)` file. Fix pattern: add `if (!r.ok) throw new Error(...)` guard before `.json()` in all fetch chains.

**Tech Stack:** React, Vitest, `@testing-library/react`, TypeScript strict

---

### TASK-001: useSessionData — response.ok guard

**Files:**
- Modify: `dashboard/src/hooks/useSessionData.ts:33`
- Test: `dashboard/src/hooks/useSessionMetrics.test.ts`

- [ ] Write failing test: when fetch returns `ok: false`, `metrics` stays null and `loading` goes false
- [ ] Run: `cd dashboard && pnpm test -- useSessionMetrics` — expect FAIL
- [ ] Implement: add `if (!r.ok) throw new Error(\`HTTP \${r.status}\`)` before `r.json()` on line 33
- [ ] Run: `cd dashboard && pnpm test -- useSessionMetrics` — expect PASS
- [ ] Commit: `fix: guard response.ok in useSessionData`

---

### TASK-002: usePermissions — response.ok guard on initial fetch

**Files:**
- Modify: `dashboard/src/hooks/usePermissions.ts:10`
- Test: `dashboard/src/hooks/usePermissions.test.ts`

- [ ] Write failing test: when `/api/permissions/pending` returns `ok: false`, permissions stays `[]`
- [ ] Run: `cd dashboard && pnpm test -- usePermissions` — expect FAIL
- [ ] Implement: add `if (!r.ok) throw new Error(...)` guard
- [ ] Run: `cd dashboard && pnpm test -- usePermissions` — expect PASS
- [ ] Commit: `fix: guard response.ok in usePermissions initial fetch`

---

### TASK-003: useRepos — response.ok guard (initial + refresh)

**Files:**
- Modify: `dashboard/src/hooks/useRepos.ts:10,20`
- Test: `dashboard/src/hooks/useRepos.test.ts` (create if missing)

- [ ] Write failing tests for both fetch paths
- [ ] Run: `cd dashboard && pnpm test -- useRepos` — expect FAIL
- [ ] Implement: add `ok` guard in both fetch chains
- [ ] Run: `cd dashboard && pnpm test -- useRepos` — expect PASS
- [ ] Commit: `fix: guard response.ok in useRepos`

---

### TASK-004: AppLayout — response.ok guard on /api/sessions/active

**Files:**
- Modify: `dashboard/src/routes/AppLayout.tsx:51-57`
- Test: `dashboard/src/routes/__tests__/AppLayout.activeSession.test.ts` (create)

- [ ] Write failing test: when `/api/sessions/active` returns `ok: false`, calls `setActiveSessionId(null)` (same as error path)
- [ ] Run: `cd dashboard && pnpm test -- AppLayout.activeSession` — expect FAIL
- [ ] Implement: add `if (!r.ok) throw new Error(...)` guard
- [ ] Run: `cd dashboard && pnpm test -- AppLayout.activeSession` — expect PASS
- [ ] Commit: `fix: guard response.ok in AppLayout active-session check`

---

### TASK-005: AppLayout — response.ok guard on startNewSession

**Files:**
- Modify: `dashboard/src/routes/AppLayout.tsx:191-202`
- Test: `dashboard/src/routes/__tests__/SessionPage.controls.test.ts` or new file

- [ ] Write failing test: when `/api/sessions/new` returns `ok: false` with JSON `{"sessionId": "bad-id"}`, `setActiveSessionId` is NOT called
- [ ] Run: `cd dashboard && pnpm test -- SessionPage.controls` — expect FAIL
- [ ] Implement: add `if (!res.ok) throw new Error(...)` after fetch
- [ ] Run: `cd dashboard && pnpm test -- SessionPage.controls` — expect PASS
- [ ] Commit: `fix: guard response.ok in startNewSession`

---

### TASK-006: usePermissions — don't update state on failed decide/decideSession

**Files:**
- Modify: `dashboard/src/hooks/usePermissions.ts:49-76`
- Test: `dashboard/src/hooks/usePermissions.test.ts`

- [ ] Write failing test: when `decide()` fetch returns `ok: false`, permission status stays `"pending"`
- [ ] Run: `cd dashboard && pnpm test -- usePermissions` — expect FAIL
- [ ] Implement: check `response.ok` before calling `setPermissions` in `decide` and `decideSession`
- [ ] Run: `cd dashboard && pnpm test -- usePermissions` — expect PASS
- [ ] Commit: `fix: don't optimistically update permissions on server error`
